#include <stdio.h>

int main() {

    int a,answer;

    for(a = 1; a < 100; a+= 2){

        answer = answer + a;
        printf("")
    }

    printf("The sum of the odd integers is %d\n",answer);

    return 0;
}